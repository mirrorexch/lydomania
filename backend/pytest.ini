[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
markers =
    asyncio: async tests (auto-handled by pytest-asyncio)
testpaths = tests
